[project]
name = "image-library-analytics"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cryptography>=48.0.0",
    "jinja2>=3.1.6",
    "opencv-python>=4.13.0.92",
    "aesthetic-predictor-v2-5>=0.1.0",
    "pyiqa>=0.1.9",
    "piexif>=1.1.3",
    "pillow>=12.2.0",
    "python-dotenv>=1.2.2",
    "rich>=15.0.0",
    "scikit-learn>=1.8.0",
    "timm>=1.0.27",
    "torch>=2.11.0",
    "transformers>=4.56.0",
    "ultralytics>=8.3.0",
    "umap-learn>=0.5.12",
    "psutil>=6.0.0",
    "pyyaml>=6.0.2",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
    "pytest>=8.0.0",
    "ruff>=0.9",
]

[tool.ruff]
target-version = "py313"
line-length = 160

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "UP", "RUF"]
ignore = [
    "RUF001",  # EN-dash/special chars in display strings are intentional (report labels)
    "RUF003",  # Same for comments with EN-dash or × in mathematical/display context
]

[tool.ruff.lint.per-file-ignores]
"extractors/composition.py" = ["E741"]    # l for line in HoughLines CV code
